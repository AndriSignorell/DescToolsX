% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute}
\alias{impute}
\title{Impute Missing Values in a Vector}
\usage{
impute(x, FUN = median, na.rm = TRUE, ...)
}
\arguments{
\item{x}{atomic vector}

\item{FUN}{function used to compute the imputation value, or a scalar value;
default is \code{median}}

\item{na.rm}{logical; whether to remove missing values before computing the
summary statistic}

\item{...}{additional arguments passed to \code{FUN}}
}
\value{
vector of the same length as \code{x}, with missing values
replaced. The type follows R's usual coercion rules: imputing a double
(the median of an integer vector, say) into an integer vector returns a
double.
}
\description{
Replaces missing values (\code{NA}) in a vector by either
a summary statistic computed from the data (e.g. mean, median)
or a user-supplied scalar value.
}
\details{
If \code{FUN} is a function, it is applied to \code{x} to compute
a single imputation value. If the function accepts an argument
named \code{na.rm}, it will be passed automatically.

Alternatively, \code{FUN} can be a single scalar value, which will
directly replace all missing values.

If \code{FUN} is a function, it must return a single value.
An error is thrown if the returned value is not scalar.

Whether \code{FUN} accepts \code{na.rm} is decided from its formal
arguments; a function taking \verb{\\dots} is assumed to pass it on.
Functions without either are called without it.
}
\examples{
x <- c(2, 3, NA, 5, 9)

# Default: median(x, na.rm=TRUE)
impute(x)

# Using mean(x, na.rm=TRUE)
impute(x, mean)

# Using trimmed mean
impute(x, function(x) meanX(x, trim=0.3, na.rm = TRUE))

# Constant replacement
impute(x, 99)

# an integer vector stays integer only if the value is one too
impute(c(2L, 3L, NA, 5L), FUN = 4L)

}
\seealso{
For direct value replacement see \code{\link[bedrock:naReplace]{bedrock::naReplace()}}.

Other impute:
\code{\link[=imputeKnn]{imputeKnn()}}
}
\concept{imputation}
\concept{impute}
\concept{missing-value}
