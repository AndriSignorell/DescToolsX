% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spearmanCor.R
\name{spearmanCor}
\alias{spearmanCor}
\title{Spearman Rank Correlation}
\usage{
spearmanCor(
  x,
  y = NULL,
  conf.level = NA,
  sides = c("two.sided", "left", "right"),
  na.rm = FALSE
)
}
\arguments{
\item{x}{either a contingency table, a two-column object
(matrix, data.frame or list), or a vector of observations
(together with \code{y})}

\item{y}{optional second vector. If \code{x} is not a vector,
\code{y} must be \code{NULL}.}

\item{conf.level}{confidence level for confidence intervals.
If \code{NA}, no confidence interval is computed.}

\item{sides}{alternative hypothesis:
\code{"two.sided"}, \code{"left"}, or \code{"right"}}

\item{na.rm}{logical; whether to remove missing values}
}
\value{
if \code{conf.level = NA}, a numeric scalar. Otherwise a named
numeric vector with elements:
\describe{
\item{\code{est}}{point estimate of Spearman's rank correlation}
\item{\code{lci}}{lower confidence interval bound}
\item{\code{uci}}{upper confidence interval bound}
}
}
\description{
Calculate Spearman correlation coefficient and its confidence interval. In
addition to the base R function \code{\link{cor}()}, frequency tables are
also accepted as arguments (i.e. actually weights are used).
}
\details{
The function calculates Spearman's rho statistic by means of \code{cor(...,
method="spearman")} when two variables \code{x} and \code{y} are supplied.
If a frequency table is provided an implementation based on SAS
documentation is used.\cr The confidence intervals are calculated via
z-Transformation.\cr
}
\examples{

pain <- as.table(matrix(c(26,  6, 26, 7, 23, 
                           9, 18, 14, 9, 23), 
                           ncol=5, byrow=TRUE, 
        dimnames=list(adverse=c("no", "yes"), dose=1:5)))

spearmanCor(pain)

spearmanCor(pain, conf.level=0.95)
  
# must be the same as
with(lapply(
       bedrock::untable(pain, 
                        colnames = c("adverse","dose")), 
       ordered), 
     spearmanCor(adverse, dose, conf.level=0.95))

}
\references{
Conover W. J. (1999) \emph{Practical Nonparametric Statistics
(3rd edition)}. Wiley
}
\seealso{
\code{\link{Association}}

Other assoc.continuous:
\code{\link[=corPart]{corPart()}},
\code{\link[=corPolychor]{corPolychor()}},
\code{\link[=findCorrX]{findCorrX()}},
\code{\link[=hoeffdingD]{hoeffdingD()}},
\code{\link[=pearsonCor]{pearsonCor()}}
}
\concept{assoc.continuous}
\concept{correlation}
\concept{rank-correlation}
