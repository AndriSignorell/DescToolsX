% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc_replace.R
\name{na_handling}
\alias{na_handling}
\alias{zeroIfNA}
\alias{naIfZero}
\alias{naIfBlank}
\alias{naVal}
\alias{naIf}
\alias{nz}
\alias{impute}
\title{Replacing NAs by Specific Values or Vice Versa}
\usage{
zeroIfNA(x)

naIfZero(x)

naIf(x, what)

naVal(x, val = "")

naIfBlank(x)

nz(x)

impute(x, FUN = function(x) median(x, na.rm = TRUE))
}
\arguments{
\item{x}{the vector x, whose \code{NA}s should be overwritten with 0s.}

\item{what}{a vector of elements to be replaced with \code{NA} in \code{x}.}

\item{val}{the value \code{NA}s should be replaced by.  Default is an empty
string (\code{""}).}

\item{FUN}{the name of a function to be used as imputation. Can as well be a
self defined function or a constant value.  Default is \code{\link{median}}.}
}
\value{
the edited vector x
}
\description{
\code{zeroIfNA()} replaces \code{NA}s in a numeric vector \code{x} with 0.
This function has the same logic as the \code{ZEROIFNULL()} function in SQL.
For values other than zero the function \code{NAVal()} can be used, which
replaces the \code{NA} values by a given value.  \code{NAIfZero()} replaces
zeros with \code{NA}. \code{NAIfBlank()} does the same, but for character
vectors.  #'
}
\examples{

z <- c(8, NA, 9, NA, 3)

zeroIfNA(z)
# [1] 8 0 9 0 3

# set 8 and 9 to NA
naIf(zeroIfNA(z), what=c(8, 9))


# Impute(z)
# [1] 8 8 9 8 3


z <- c("a", NA, "", NA, "k")

naVal(z)
# [1] "a" "" "" "" "k"


}
\seealso{
\code{\link{replace}}
}
\author{
Andri Signorell \href{mailto:andri@signorell.net}{andri@signorell.net}
}
\keyword{manip}
