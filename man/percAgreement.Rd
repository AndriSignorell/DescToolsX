% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percAgreement.R
\name{percAgreement}
\alias{percAgreement}
\title{Percent Agreement with Design-Based SE and CI}
\usage{
percAgreement(
  x,
  y = NULL,
  conf.level = NA,
  sides = c("two.sided", "left", "right"),
  input = c("auto", "confusion", "ratings"),
  fpc = 0,
  output = c("def", "ext"),
  ...
)
}
\arguments{
\item{x}{a confusion matrix or a matrix or data frame of ratings with
subjects in rows and raters in columns}

\item{y}{optional second rating vector used to construct a confusion matrix}

\item{conf.level}{confidence level of the interval. If set to \code{NA}
(the default), only the point estimate is returned.}

\item{sides}{character string specifying the sidedness of the confidence
interval (one of \code{"two.sided"} (default), \code{"left"} or
\code{"right"}). See \code{\link{ConfidenceIntervals}}. A proportion of
agreement lies in \eqn{[0, 1]}, so the open side is reported at that
boundary rather than at \eqn{\pm\infty}.}

\item{input}{character string specifying the input format:
\code{"auto"}, \code{"confusion"}, or \code{"ratings"}}

\item{fpc}{finite population correction, the sampling fraction \eqn{n/N}
in \eqn{[0, 1)} (default \code{0})}

\item{output}{output format, either \code{"def"} (default) or
\code{"ext"} for extended results}

\item{...}{must be empty. Named arguments are rejected rather than
silently ignored.}
}
\value{
if \code{output = "def"} and \code{conf.level = NA}, a numeric
scalar; otherwise a named numeric vector with elements:
\describe{
\item{\code{est}}{proportion of agreement}
\item{\code{lci}}{lower confidence interval bound}
\item{\code{uci}}{upper confidence interval bound}
}

if \code{output = "ext"}, a list with the elements \code{est},
\code{se}, \code{ci} (the named triple above), \code{n},
\code{nPairable} and \code{method}.
}
\description{
Computes the proportion of agreement (percent agreement) along with
design-based standard errors and confidence intervals following
Klein and Gwet.
}
\details{
For a confusion matrix the agreement indicator of a subject is 1 on the
diagonal and 0 elsewhere. For a ratings matrix the subject-wise agreement
is the proportion of agreeing rater \emph{pairs},
\deqn{p_{o,i} = \frac{\sum_j n_{ij}(n_{ij}-1)}{m_i (m_i - 1)},}
where \eqn{n_{ij}} is the number of raters who assigned subject \eqn{i} to
category \eqn{j} and \eqn{m_i} the number of non-missing ratings for that
subject. Subjects with fewer than two ratings carry no information about
agreement and are excluded (they still count towards \eqn{n} in the
variance, following Gwet).
}
\seealso{
Other assoc.agreement:
\code{\link[=ccc]{ccc()}},
\code{\link[=cohenKappa]{cohenKappa()}},
\code{\link[=cronbachAlpha]{cronbachAlpha()}},
\code{\link[=icc]{icc()}},
\code{\link[=kappaM]{kappaM()}},
\code{\link[=krippAlpha]{krippAlpha()}},
\code{\link[=pabak]{pabak()}},
\code{\link[=randolphKappa]{randolphKappa()}}
}
\concept{agreement}
\concept{assoc.agreement}
\concept{categorical-agreement}
